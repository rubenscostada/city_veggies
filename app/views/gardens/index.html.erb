<div>
  <div class="banner-content">
    <div class= "container">
      <h1>Veggies in the city</h1>
      <p>Rent your place to grow delicious food</p>
      <%= link_to "Add a garden", new_garden_path, class: "btn btn-primary btn-lg" %>
    </div>
  </div>
</div>
<%= render 'shared/searchform' %>
<div class="container">
  <% @gardens.each do |garden| %>
  <%= link_to garden_path(garden.id) do %>
    <div class="row">
      <div class="col-12">
        <div class="card-product">
            <img src="https://hips.hearstapps.com/hmg-prod.s3.amazonaws.com/images/garden-layout-1582574581.jpg" />
            <div class="card-product-infos">
               <h2> <%=garden.name %> </h2>
              <h5> <%= garden.country %> </h5>
              <p><%= garden.description %> </p>
              <p><strong><%= garden.price_per_month %> € </strong></p>
            </div>
        </div>
      </div>
    </div>
    <% end %>
  <% end %>
</div>
